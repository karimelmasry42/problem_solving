#include <stdio.h>
void read(int a, int *b) { scanf("%d %d", a, *b); }
int main(void) {
  // int m, x, y;
  int a, *b;
  scanf("%d %d", a, &b);
  // if (x > y) {
  //   m = x;
  //   x = y;
  //   y = m;
  // } else
  //   m = x % y;
  // printf("m=%d", m);
}